Django==6.0.2
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
drf-yasg==1.21.15
python-decouple==3.8
dj-database-url==3.1.2
psycopg2-binary==2.9.11
gunicorn==26.0.0
whitenoise[brotli]==6.12.0
Pillow==12.2.0

# ============================================================
# REVIEW — READ THIS THEN DELETE FROM THIS LINE TO THE END
# ============================================================
#
# What each class or function does and why it was written that way
#
# requirements.txt lists the Python packages Render installs before running
# the Django application.
#
# Django is the web framework used by the project.
#
# djangorestframework powers the API views, serializers, permissions, and
# test client.
#
# djangorestframework-simplejwt provides JWT login, refresh, and verify views.
#
# django-cors-headers handles CORS headers for frontend clients.
#
# drf-yasg generates Swagger and ReDoc API documentation.
#
# python-decouple reads environment variables and .env values.
#
# dj-database-url turns DATABASE_URL into Django database settings.
#
# psycopg2-binary lets Django connect to PostgreSQL on Render.
#
# gunicorn runs the Django WSGI application in production.
#
# whitenoise serves static files from the Django app on Render. The brotli
# extra adds compressed static file support.
#
# Pillow supports ImageField for menu item images.
#
# Important decisions that were made and why
#
# Only project dependencies are listed instead of every package in the local
# virtual environment. This keeps deployment smaller and clearer.
#
# Versions are pinned so Render installs predictable package versions.
#
# What you should read and understand before you review the code
#
# Read pip requirements file syntax.
#
# Read Render Python dependency installation.
#
# Read why production Django apps usually run with Gunicorn and WhiteNoise.
#
# ============================================================
# END OF REVIEW
# ============================================================
